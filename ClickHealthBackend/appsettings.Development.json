{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  // Authentication section intentionally omitted in Development
  // so values from appsettings.json are used.
}
